Basically 16 bit operand inputs are given .
then they are added and the 16 bit output is stored .
depending on the result , the carry flag and the zero flag are modified .

problems : (1) gives output in the next clock state
